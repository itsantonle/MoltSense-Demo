import { CellConfigPage } from '@/components/cell-config/cell-config-page';

export const metadata = {
  title: 'Cell Config - MoltSense',
  description: 'Configure the individual thresholds for a single cell',
};

export default function Page() {
  return <CellConfigPage />;
}
